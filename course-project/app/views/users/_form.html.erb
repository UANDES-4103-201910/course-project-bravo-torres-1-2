<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%if current_user.useris == "Admin"%>
  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
  <%elsif current_user.useris == "SuperAdmin"%>
      <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
    </div>
      <div class="field">
    <%= form.label :geofence_id %>
    <%= form.text_field :geofence_id %>
  </div>
    <div class="field">
    <%= form.label :useris %>
    <%= form.text_field :useris %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
  <%end%>
<%end%>

